function calcularArea (base: number, altura: number): number {
    const area = base * altura;
    return area;

};
// Adcione os valor valores desejados em "a" e "b"  para caular a area.

let b = 5
let a = 2

const resultado = calcularArea(b, a);
console.log(resultado);